import React from "react";
import Collections from "./_components/collections";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <Collections />
    </div>
  );
};

export default page;
