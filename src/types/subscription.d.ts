interface SubscriptionResponse {
    data: string;
}

interface CancelSubscriptionResponse {
    data: {
        message: string;
    };
}

interface AddCardResponse {
    data: {
        message: string;
    };
}